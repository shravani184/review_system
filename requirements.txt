# --- Web framework ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9        # multipart/form-data file uploads

# --- Validation / config ---
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0

# --- Static analysis (symbolic layer = source of truth) ---
pylint>=3.0
bandit>=1.7

# --- LLM client ---
httpx>=0.27                    # OpenAI HTTP calls (and FastAPI TestClient)

# --- Testing ---
pytest>=8.0
